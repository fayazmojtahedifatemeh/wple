# Database (automatically configured by Replit)
DATABASE_URL=

# Email Notifications (optional - set these to enable email alerts)
# If not set, the application will still function but won't send email notifications
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password
EMAIL_FROM=your-email@gmail.com
NOTIFICATION_EMAIL=recipient@example.com

# Gemini AI (for automatic product categorization)
GEMINI_API_KEY=your-gemini-api-key
